services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - MODEL_DIR=app/model
      - MODEL_VERSION=1.0.0

  frontend:
    build: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
